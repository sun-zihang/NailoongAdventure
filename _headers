# 注意：不要给 /game/* 加 COOP/COEP，否则 .unityweb 子资源会被
# Cross-Origin-Embedder-Policy: require-corp 拦截（浏览器要求嵌入
# 资源必须带 CORP 头，而 Cloudflare Pages 默认不给静态文件加）。
# 本 Demo 未启用 WebAssembly 多线程 / SharedArrayBuffer，不需要 COEP。

# 禁止 Cloudflare / 中间代理对 .unityweb 做自动转码或压缩。
# Unity 的 Decompression Fallback 期望拿到原始 .unityweb 字节，
# 由 JS 端自行解压；若 CDN 额外加 Content-Encoding 头，会触发
# "Content-Encoding header is incorrectly enabled" 错误。
/game/Build/*.unityweb
  Cache-Control: public, max-age=86400, no-transform
